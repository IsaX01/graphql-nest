apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
data:
  POSTGRES_USER: {{ base64_encode 'isax01' }}
  POSTGRES_PASSWORD: {{ base64_encode 'isax' }}
  POSTGRES_DB: {{ base64_encode 'jobsGraphQL' }}

